{
  "name": "bra-b-backend",
  "version": "0.1.0",
  "description": "Backend API for bra-B application",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "deploy:dev": "wrangler deploy --env development"
  },
  "dependencies": {
    "hono": "^4.7.4",
    "wrangler": "^3.34.2"
  }
}
